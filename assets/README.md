# Assets

このフォルダにはLPで使用する画像を配置します。

## profile image

テスト用に、勝目麻希さんのプロフィール写真を `maki-profile.jpg` として配置予定です。

このチャット上では画像ファイルのGitHub直接アップロードができない場合があるため、必要に応じてGitHub画面からこのフォルダへ画像をアップロードしてください。

推奨ファイル名:

- `maki-profile.jpg`

LP内では以下のパスで参照しています。

```html
assets/maki-profile.jpg
```
